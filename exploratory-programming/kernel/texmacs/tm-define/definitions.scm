(define-module (kernel texmacs tm-define definitions)
  #:use-module (kernel texmacs tm-define)
  #:use-module (oop goops)
  #:duplicates (merge-generics merge-accessors replace warn-override-core warn last))
;; Nothing here. Just defining the module.
